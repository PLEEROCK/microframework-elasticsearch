/**
 * Configuration for elastic-search module.
 */
export interface ElasticSearchModuleConfig {

    /**
     * Elasticsearch host, connection with which will be established.
     */
    host: string;

    /**
     * Logging mode.
     */
    log: string;

}
